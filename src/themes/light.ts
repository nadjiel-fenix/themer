import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#4169e1" },
    secondary: { main: "#e1b941" },
  },
});

export default theme;
